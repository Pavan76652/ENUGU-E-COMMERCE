import ContactActions from '../contact/ContactActions';

const ContactButtons = ({ message = 'Hi ENUGU, I have a custom design enquiry.' }) => (
  <ContactActions message={message} />
);

export default ContactButtons;
